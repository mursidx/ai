import { useEffect,useState } from 'react';import api from '../../lib/api';
export default function Expenses(){const [rows,setRows]=useState([]);const [f,setF]=useState({category:'Rent',amount:0,expense_date:new Date().toISOString().slice(0,10)});const load=()=>api.get('/expenses').then(r=>setRows(r.data));useEffect(load,[]);
return <div><h1 className='text-2xl font-bold mb-3'>Expenses</h1><div className='bg-white p-3 rounded shadow'><input className='border p-2' value={f.category} onChange={e=>setF({...f,category:e.target.value})}/><input className='border p-2 ml-2' type='number' value={f.amount} onChange={e=>setF({...f,amount:+e.target.value})}/><button className='bg-blue-700 text-white px-3 ml-2 rounded' onClick={async()=>{await api.post('/expenses',f);load();}}>Add</button></div><table className='w-full bg-white mt-3'><thead><tr><th>Date</th><th>Category</th><th>Amount</th></tr></thead><tbody>{rows.map(r=><tr key={r.id}><td>{r.expense_date}</td><td>{r.category}</td><td>{r.amount}</td></tr>)}</tbody></table></div>}
